AI Resume Critiquer


To use this application, get an OpenAI API key from https://platform.openai.com/api-keys, create a .env file in the project folder with OPENAI_API_KEY=your_key_here, and run "uv run streamlit run main.py."


